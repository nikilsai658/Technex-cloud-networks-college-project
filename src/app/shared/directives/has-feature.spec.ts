import { HasFeature } from './has-feature';

describe('HasFeature', () => {
  it('should create an instance', () => {
    const directive = new HasFeature();
    expect(directive).toBeTruthy();
  });
});
